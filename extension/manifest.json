{
  "manifest_version": 3,
  "name": "Region Proxy Switcher",
  "version": "1.0.0",
  "description": "One-click toggle: route Chrome through a local SOCKS5 tunnel (ssh -D) to a regional exit, or go direct.",
  "permissions": ["proxy", "storage"],
  "background": { "service_worker": "background.js" },
  "action": { "default_title": "Region Proxy: OFF (click to enable)" }
}
